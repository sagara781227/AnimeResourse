@model IEnumerable<AnimeResource.Models.mark>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.anime.nameanim)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user.login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mark1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.anime.nameanim)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user.login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mark1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_mark }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_mark }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_mark })
        </td>
    </tr>
}

</table>
